<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Age Widget</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600&display=swap" rel="stylesheet">
    <style>
        /* Base Styles */
        body {
            font-family: 'Inter', sans-serif;
            background: transparent;
            color: #000;
            text-align: center;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            transition: background 0.3s, color 0.3s;
        }
        .widget {
            background: #ffffff;
            border-radius: 12px;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            padding: 24px;
            width: 320px;
            text-align: center;
            border: 1px solid rgba(0, 0, 0, 0.1);
            transition: background 0.3s, box-shadow 0.3s;
        }
        h1 {
            font-size: 24px;
            margin: 0;
            font-weight: 600;
            color: #333;
            transition: color 0.3s;
        }
        .message {
            font-size: 14px;
            margin: 16px 0;
            color: #666;
            transition: color 0.3s;
        }
        .age {
            font-size: 20px;
            margin: 10px 0;
            font-weight: 500;
            color: #333;
            transition: color 0.3s;
        }
        .time {
            font-size: 16px;
            margin-top: 10px;
            color: #666;
            transition: color 0.3s;
        }
        .sub-text {
            font-size: 12px;
            color: #888;
            margin-top: 16px;
            transition: color 0.3s;
        }
        .blink {
            color: #ff4081;
            animation: blink 1s infinite;
        }
        @keyframes blink {
            0% { opacity: 1; }
            50% { opacity: 0; }
            100% { opacity: 1; }
        }

        /* Theme Toggle */
        .theme-toggle {
            position: absolute;
            top: 16px;
            right: 16px;
            background: #f0f0f0;
            border: none;
            border-radius: 50%;
            width: 32px;
            height: 32px;
            cursor: pointer;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: background 0.3s;
        }
        .theme-toggle:hover {
            background: #e0e0e0;
        }

        /* Dark Theme */
        body.dark {
            background: #121212;
            color: #fff;
        }
        body.dark .widget {
            background: #1e1e1e;
            border-color: rgba(255, 255, 255, 0.1);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.5);
        }
        body.dark h1 {
            color: #00ffcc;
        }
        body.dark .message,
        body.dark .time,
        body.dark .sub-text {
            color: #aaa;
        }
        body.dark .age {
            color: #00ffcc;
        }
        body.dark .theme-toggle {
            background: #333;
            color: #fff;
        }
        body.dark .theme-toggle:hover {
            background: #444;
        }

        /* Modern Themes */
        body.theme-1 .widget {
            background: linear-gradient(135deg, #ff9a9e, #fad0c4);
        }
        body.theme-2 .widget {
            background: linear-gradient(135deg, #a1c4fd, #c2e9fb);
        }
        body.theme-3 .widget {
            background: linear-gradient(135deg, #84fab0, #8fd3f4);
        }
        body.theme-4 .widget {
            background: linear-gradient(135deg, #fbc2eb, #a6c1ee);
        }
        body.theme-5 .widget {
            background: linear-gradient(135deg, #ffecd2, #fcb69f);
        }
    </style>
</head>
<body class="light">
    <div class="widget">
        <button class="theme-toggle" onclick="toggleTheme()">🌓</button>
        <h1>Hey Varun!</h1>
        <div class="message">Today you have lived for:</div>
        <div class="age" id="age"></div>
        <div class="time" id="time"></div>
        <div class="sub-text">Updating every second...<span class="blink">⏳</span></div>
    </div>

    <script>
        // Calculate Age and Time
        function calculateAge() {
            const birthDate = new Date('1996-11-26T10:55:00'); // Your birthdate and time
            const now = new Date();
            
            const diffInMilliseconds = now - birthDate;
            const totalSeconds = Math.floor(diffInMilliseconds / 1000);
            const totalMinutes = Math.floor(totalSeconds / 60);
            const totalHours = Math.floor(totalMinutes / 60);
            const totalDays = Math.floor(totalHours / 24);
            const totalWeeks = Math.floor(totalDays / 7);
            
            const years = Math.floor(totalDays / 365.25);
            const months = Math.floor((totalDays % 365.25) / 30);
            const days = totalDays % 365.25 % 30;
            const hours = totalHours % 24;
            const minutes = totalMinutes % 60;
            const seconds = totalSeconds % 60;
            
            // Display the age and time
            document.getElementById('age').innerHTML = `${years} years, ${months} months, ${days} days`;
            document.getElementById('time').innerHTML = `${hours} hours, ${minutes} minutes, ${seconds} seconds, ${totalWeeks} weeks`;
        }

        setInterval(calculateAge, 1000); // Updates every second
        calculateAge(); // Initial call to set the age

        // Theme Toggle
        function toggleTheme() {
            const body = document.body;
            if (body.classList.contains('light')) {
                body.classList.remove('light');
                body.classList.add('dark');
            } else if (body.classList.contains('dark')) {
                body.classList.remove('dark');
                body.classList.add('theme-1');
            } else if (body.classList.contains('theme-1')) {
                body.classList.remove('theme-1');
                body.classList.add('theme-2');
            } else if (body.classList.contains('theme-2')) {
                body.classList.remove('theme-2');
                body.classList.add('theme-3');
            } else if (body.classList.contains('theme-3')) {
                body.classList.remove('theme-3');
                body.classList.add('theme-4');
            } else if (body.classList.contains('theme-4')) {
                body.classList.remove('theme-4');
                body.classList.add('theme-5');
            } else {
                body.classList.remove('theme-5');
                body.classList.add('light');
            }
        }
    </script>
</body>
</html>
